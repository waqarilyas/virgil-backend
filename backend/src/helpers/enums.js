const FRIEND_STATUS = {
  friend: "FRIEND",
  anon: "ANONYMOUS",
  requested: "REQUESTED",
  currentUser: "CURRENT_USER",
};

const ENVIRONMENTS = {
  staging: 'STAGING',
  production: 'PRODUCTION'
}

module.exports = {
  FRIEND_STATUS,
  ENVIRONMENTS
};
