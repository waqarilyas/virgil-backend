image: node:latest

before_script:
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl

stages:
    - staging

staging:
    type: deploy
    stage: staging
    image: ruby:latest
    script:
        - cd backend
        - dpl --provider=heroku --app=virgil-dev --api-key=$HEROKU_API_KEY
    only:
        - staging
